# 이 저장소에서 작업할 때

## 커밋

- **커밋 메시지에 `Co-Authored-By` 트레일러를 넣지 마세요.** 저자는
  DH-LDH 단독으로 남깁니다. 기본 동작으로 붙이던 것을 2026-08-13 에
  제거했고, 앞으로도 붙이지 않습니다.
- 커밋·push 는 사용자가 요청할 때만 합니다.

## 문서

- `HANDOFF.md` 가 이 프로젝트의 실질적 기억입니다. 작업을 끝낼 때 갱신까지가
  그 작업의 일부입니다. 틀린 내용을 발견하면 반드시 고치세요 — 남은 오류는
  다음 세션을 그대로 오도합니다.
- `README.md` 는 "어떻게 돌리는가"만 답합니다. 왜 그런지, 어디서 막혔는지,
  검증 수치는 전부 `HANDOFF.md` 로 보냅니다. 두 곳에 같은 내용을 쓰지 마세요.

## 실기 작업

- 로봇을 실제로 움직이는 명령(teleop, `--spin` 등)은 **실행 전에 반드시
  사용자에게 확인**을 받으세요. 사용자가 바퀴를 띄울 수 없는 상황일 수 있습니다.
- `sudo` 가 필요한 명령은 이 환경에서 실행되지 않습니다. 명령문을 채팅 본문에
  코드블록으로 그대로 적어 사용자가 복사할 수 있게 하세요.

# TETRA-DSV-S USB-serial port pinning.
#
# Why this is not optional: the robot has at least three USB-serial devices
# (drive board CN20, power/sensor board CN24, iAHRS IMU). /dev/ttyUSB0..2 are
# handed out in enumeration order, which changes between boots and whenever a
# cable is re-seated. Bind the wrong one and the drive node opens the IMU,
# writes BV frames into it, and reports nothing but read timeouts.
#
# Install on the Jetson:
#     sudo cp 99-tetra-serial.rules /etc/udev/rules.d/
#     sudo udevadm control --reload-rules && sudo udevadm trigger
#
# FILL IN THE SERIALS FIRST. Find them with:
#     udevadm info -a -n /dev/ttyUSB0 | grep -E 'idVendor|idProduct|serial'
# (drive board done 2026-08-12; power/sensor board and IMU still need this —
# plug each adapter in one at a time and rerun the command above)
#
# If the adapters share a vendor/product AND have no unique serial - common
# with cheap CH340 chips - match on the physical USB port path instead:
#     KERNELS=="1-2.3:1.0"    (from: udevadm info -a -n /dev/ttyUSB0 | grep KERNELS)
# That binds to the socket rather than the device, so it survives swaps but
# breaks if the cable moves to another socket. Pick whichever is more stable
# for this robot and delete the other line.

# Drive board (CN20, 115200) -> /dev/tetra_drive
# Filled in 2026-08-12: FTDI FT232R adapter, currently seated on CN20.
#     udevadm info -a -n /dev/ttyUSB0 | grep -E 'idVendor|idProduct|serial'
#
# latency_timer=1: FTDI's default (16ms) buffers incoming bytes and flushes
# them to the host in chunks, so a single BV reply can arrive split across
# two reads with a multi-ms gap in between - indistinguishable on the wire
# from the reply actually ending there. Confirmed 2026-08-13: tetra_drive_node
# saw truncated BV replies (2-3 bytes of a 16-byte frame) at the default
# timer; dropping it to 1ms fixed it immediately. Without this line the value
# resets to 16 on every unplug/reboot.
SUBSYSTEM=="tty", ATTRS{idVendor}=="0403", ATTRS{idProduct}=="6001", ATTRS{serial}=="A9R3ADK4", SYMLINK+="tetra_drive", MODE="0666", ATTR{latency_timer}="1"

# Power / sensor board (CN24, 115200) -> /dev/tetra_power
# TODO: not yet filled in — this board's USB-serial adapter isn't plugged in
# right now. Plug it in, then: udevadm info -a -n /dev/ttyUSB0 | grep -E 'idVendor|idProduct|serial'
SUBSYSTEM=="tty", ATTRS{idVendor}=="XXXX", ATTRS{idProduct}=="XXXX", ATTRS{serial}=="CHANGEME_POWER", SYMLINK+="tetra_power", MODE="0666"

# iAHRS IMU -> /dev/tetra_imu
# TODO: not yet filled in — same as above, plug in and rerun udevadm.
# NOTE: if this also turns out to be an FTDI FT232R (0403:6001, same as the
# drive board), that's fine as long as its serial differs — FTDI programs a
# unique serial per chip, so matching stays unambiguous.
SUBSYSTEM=="tty", ATTRS{idVendor}=="XXXX", ATTRS{idProduct}=="XXXX", ATTRS{serial}=="CHANGEME_IMU", SYMLINK+="tetra_imu", MODE="0666"
